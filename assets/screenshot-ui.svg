<svg width="800" height="500" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&amp;display=swap');
      .font { font-family: 'Roboto', sans-serif; }
      .blur { filter: url(#blur); }
    </style>
    <filter id="blur">
      <feGaussianBlur in="SourceGraphic" stdDeviation="3" />
    </filter>
    <filter id="shadow" x="-20%" y="-20%" width="140%" height="140%">
      <feDropShadow dx="0" dy="10" stdDeviation="10" flood-opacity="0.3"/>
    </filter>
  </defs>

  <!-- 1. 背景：模拟 Google AI Studio 界面 -->
  <rect width="100%" height="100%" fill="#f0f4f9" />
  
  <!-- 侧边栏 -->
  <rect x="0" y="0" width="200" height="500" fill="#ffffff" />
  <rect x="20" y="80" width="160" height="15" rx="4" fill="#e1e3e1" />
  <rect x="20" y="110" width="120" height="15" rx="4" fill="#e1e3e1" />
  <rect x="20" y="140" width="140" height="15" rx="4" fill="#e1e3e1" />

  <!-- 聊天区域背景 -->
  <!-- User Bubble -->
  <rect x="450" y="100" width="300" height="60" rx="12" fill="#d3e3fd" />
  <rect x="480" y="120" width="240" height="10" rx="2" fill="#444746" opacity="0.3" />
  <!-- AI Bubble -->
  <rect x="250" y="200" width="400" height="120" rx="12" fill="#ffffff" stroke="#c4c7c5" />
  <rect x="280" y="230" width="340" height="10" rx="2" fill="#444746" opacity="0.2" />
  <rect x="280" y="250" width="300" height="10" rx="2" fill="#444746" opacity="0.2" />
  <rect x="280" y="270" width="200" height="10" rx="2" fill="#444746" opacity="0.2" />

  <!-- 2. 遮罩层 (Overlay) -->
  <rect width="100%" height="100%" fill="rgba(0,0,0,0.7)" />

  <!-- 3. v14 右上角悬浮入口按钮 -->
  <g transform="translate(620, 80)" filter="url(#shadow)">
    <rect x="0" y="0" width="160" height="44" rx="22" fill="#1a73e8" />
    <text x="20" y="28" fill="white" font-size="16" font-weight="bold" class="font">🚀 导出对话 (v14)</text>
  </g>

  <!-- 4. v14 中央弹窗 -->
  <g transform="translate(200, 125)" filter="url(#shadow)">
    <!-- 弹窗白底 -->
    <rect x="0" y="0" width="400" height="250" rx="16" fill="#ffffff" />
    
    <!-- 标题 -->
    <text x="200" y="50" text-anchor="middle" fill="#202124" font-size="24" font-weight="bold" class="font">正在采集...</text>
    
    <!-- 状态文本 -->
    <text x="200" y="90" text-anchor="middle" fill="#5f6368" font-size="15" class="font">正在向下滚动并抓取内容。</text>
    <text x="200" y="115" text-anchor="middle" fill="#5f6368" font-size="14" class="font">
      按 <tspan font-weight="bold" fill="#444">ESC</tspan> 键可强制停止并保存。
    </text>
    
    <!-- 计数器 (动态感) -->
    <text x="200" y="180" text-anchor="middle" fill="#1a73e8" font-size="48" font-weight="bold" class="font">42</text>
    <text x="245" y="180" fill="#80868b" font-size="14" class="font">条</text>
    
    <!-- 底部装饰条 -->
    <rect x="150" y="210" width="100" height="4" rx="2" fill="#e0e0e0" />
    <rect x="150" y="210" width="60" height="4" rx="2" fill="#1a73e8">
      <animate attributeName="width" from="0" to="100" dur="2s" repeatCount="indefinite" />
    </rect>
  </g>
</svg>
